<UserControl x:Class="MVVM_Bonus.View.GeneralView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200">

    <UserControl.Resources>
        <!-- Fade-in Animation for Profile Card -->
        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
            <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,20,0,0" To="0,0,0,0" Duration="0:0:0.5">
                <ThicknessAnimation.EasingFunction>
                    <QuadraticEase EasingMode="EaseOut"/>
                </ThicknessAnimation.EasingFunction>
            </ThicknessAnimation>
        </Storyboard>

        <Style x:Key="InfoBoxStyle" TargetType="Border">
            <Setter Property="Background" Value="#221738"/>
            <Setter Property="BorderBrush" Value="#3D2B5C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <!-- Outlined Button Style -->
        <Style x:Key="OutlinedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#00B8A9"/>
            <Setter Property="BorderBrush" Value="#00B8A9"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#00B8A9"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Filled Gradient Button Style -->
        <Style x:Key="FilledButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="8">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#00B8A9" Offset="0"/>
                                    <GradientStop Color="#00D4AA" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ListBoxItem Style -->
        <Style x:Key="WorkerListItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemBorder" Background="#2A1D42" BorderBrush="#3D2B5C" BorderThickness="1" CornerRadius="10" Margin="0,0,0,10" Padding="15" Cursor="Hand">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Accent Bar -->
                                <Border x:Name="AccentBar" Grid.Column="0" Width="3" Background="Transparent" CornerRadius="1.5" Margin="-15,-15,12,-15"/>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <ContentPresenter/>
                                </StackPanel>
                                
                                <TextBlock Grid.Column="2" Text="→" Foreground="#00B8A9" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Margin="10,0,0,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#00B8A9"/>
                                <Setter TargetName="ItemBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00B8A9" BlurRadius="10" ShadowDepth="0" Opacity="0.3"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="AccentBar" Property="Background" Value="#00B8A9"/>
                                <Setter TargetName="ItemBorder" Property="Background" Value="#2D2146"/>
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#00B8A9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#1A1225">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Header Bar -->
        <Border Grid.Row="0" Background="#221738" Padding="24,16">
            <DockPanel LastChildFill="False">
                <!-- Title -->
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                    <Ellipse Width="10" Height="10" Fill="#00B8A9" Margin="0,0,12,0"/>
                    <TextBlock Text="BMO Quality Bonus" Foreground="#FFFFFF" FontSize="20" FontWeight="Bold"/>
                </StackPanel>

                <!-- Search Bar -->
                <Border DockPanel.Dock="Right" Background="#2A1D42" BorderBrush="#3D2B5C" BorderThickness="1" CornerRadius="10" Padding="12,8" Width="300">
                    <DockPanel LastChildFill="True">
                        <TextBlock Text="🔍" Foreground="#8B7AAF" DockPanel.Dock="Left" FontSize="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBox Background="Transparent" BorderThickness="0" Foreground="#FFFFFF" FontSize="14" VerticalContentAlignment="Center"
                                 Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}"/>
                    </DockPanel>
                </Border>
            </DockPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT COLUMN: Selected Employee Profile Card -->
            <Border Grid.Column="0" Background="#2A1D42" CornerRadius="14" Padding="24">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="5" Opacity="0.3"/>
                </Border.Effect>
                <Border.Triggers>
                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                        <BeginStoryboard Storyboard="{StaticResource FadeInStoryboard}"/>
                    </EventTrigger>
                </Border.Triggers>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Label -->
                        <TextBlock Text="S E L E C T E D   E M P L O Y E E" Foreground="#00B8A9" FontSize="11" FontWeight="Bold" Margin="0,0,0,20"/>
                        
                        <!-- Info Rows -->
                        <Border Style="{StaticResource InfoBoxStyle}">
                            <StackPanel>
                                <TextBlock Text="Name" Foreground="#8B7AAF" FontSize="12" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding SelectedWorker.Name}" Foreground="#FFFFFF" FontSize="15" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource InfoBoxStyle}">
                            <StackPanel>
                                <TextBlock Text="Function" Foreground="#8B7AAF" FontSize="12" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding SelectedWorker.Role}" Foreground="#FFFFFF" FontSize="15" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource InfoBoxStyle}">
                            <StackPanel>
                                <TextBlock Text="Contract Type" Foreground="#8B7AAF" FontSize="12" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding SelectedWorker.Contract}" Foreground="#FFFFFF" FontSize="15" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource InfoBoxStyle}" Margin="0,0,0,24">
                            <StackPanel>
                                <TextBlock Text="Last Bonus Date" Foreground="#8B7AAF" FontSize="12" Margin="0,0,0,4"/>
                                <TextBlock Text="00/00/00" Foreground="#FFFFFF" FontSize="15" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>

                        <!-- Action Buttons -->
                        <Button Content="📊 View History" Style="{StaticResource OutlinedButtonStyle}" Command="{Binding GetPersonsBonus}"/>
                        <Button Content="➕ New Bonus" Style="{StaticResource FilledButtonStyle}" Command="{Binding InsertPersonsBonus}"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- RIGHT COLUMN: Employee List -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- List Header -->
                <Border Grid.Row="0" Background="#221738" CornerRadius="10,10,0,0" Padding="20,16">
                    <TextBlock Text="Team Members" Foreground="#FFFFFF" FontSize="16" FontWeight="Bold"/>
                </Border>

                <!-- ListBox -->
                <ListBox Grid.Row="1" ItemsSource="{Binding FilteredListPerson}" SelectedItem="{Binding SelectedWorker}" 
                         Background="Transparent" BorderThickness="0" Padding="0,15,0,0" 
                         ItemContainerStyle="{StaticResource WorkerListItemStyle}"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" Foreground="#FFFFFF" FontSize="14" FontWeight="Bold" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding Role}" Foreground="#B8A9D4" FontSize="12"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#221738" Padding="20,12">
            <TextBlock Text="Tip: Select an employee to view and manage their bonus information" Foreground="#8B7AAF" FontSize="12" FontStyle="Italic"/>
        </Border>
    </Grid>
</UserControl>
