<UserControl x:Class="MVVM_Bonus.View.GeneralView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="1*"/>
 
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Grid.RowSpan="2" Margin="25">
            <TextBlock Text="Name: " Foreground="white" FontWeight="Bold" FontSize="20"/>
            <TextBlock Text="{Binding SelectedWorker.P_Name}" Foreground="white" FontSize="20" Margin="10,0,0,5"/>
            <TextBlock Text="Function: " Foreground="white" FontWeight="Bold"  FontSize="20"/>
            <TextBlock Text="{Binding SelectedWorker.P_Role}" Foreground="white" FontSize="20" Margin="10,0,0,5"/>
            <TextBlock Text="Contract: " Foreground="white" FontWeight="Bold"   FontSize="20"/>
            <TextBlock Text="{Binding SelectedWorker.P_Contract}" Foreground="white" FontSize="20" Margin="10,0,0,5"/>
            <TextBlock Text="Last Bonus Date: " FontWeight="Bold" Foreground="white" FontSize="20"/>
            <TextBlock Text="00/00/00" Foreground="white" FontSize="20" Margin="10,0,0,5"/>
        </StackPanel>
        <StackPanel Grid.Row="2" VerticalAlignment="Bottom">
            <Button Content="View bonus" Style="{StaticResource TransformedButtonBorder}" Command="{Binding GetPersonsBonus}" />
            <Button Content="New bonus"  Style="{StaticResource TransformedButtonBorder}" Command="{Binding InsertPersonsBonus}"/>
            <Button Content="Others"     Style="{StaticResource TransformedButtonBorder}" Command="{Binding }"/>
        </StackPanel>

        
        <Grid Grid.Column="1" Grid.RowSpan="3" Margin="5"  Height="auto">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.5*"/>
       
                <RowDefinition Height="3*"/>
            </Grid.RowDefinitions>
            <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,5" HorizontalAlignment="Right">
                <TextBlock Text="🔍" Foreground="white" DockPanel.Dock="Left" FontWeight="Bold" VerticalAlignment="Center" FontSize="18" Margin="10,0,10,0"/>
                <Border Background="White" CornerRadius="15" Width="300" Height="35" >
                    <TextBox Background="Transparent" Foreground="#F48473" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" VerticalContentAlignment="Center" FontSize="18"/>
                </Border>
            </DockPanel>
            <ListBox Grid.Row="2" ItemsSource="{Binding Path=FilteredListPerson}" HorizontalContentAlignment="Stretch" SelectedItem="{Binding SelectedWorker, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Height="auto" Panel.ZIndex="99" >
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="5" Margin="5" Background="#4CFFFFFF">
                            <TextBlock  Text="{Binding P_Name}" Foreground="White" VerticalAlignment="Center" Margin="10,0,0,0"/>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

        </Grid>
    </Grid>
</UserControl>
