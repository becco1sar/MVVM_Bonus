<UserControl x:Class="MVVM_Bonus.View.GeneralView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Border BorderThickness="1" CornerRadius="5" Background="#F48473" Height="100" Margin="5">
            <Button Content="View bonus" FontSize="20" FontWeight="Bold" Background="Transparent" Foreground="white" BorderBrush="Transparent"  Command="{Binding GetPersonsBonus}" />
        </Border>
        <Border BorderThickness="1" CornerRadius="5" Background="#F48473" Height="100" Grid.Row="1" Margin="5">
            <Button Content="New bonus"  FontSize="20" FontWeight="Bold" Background="Transparent" Foreground="white" BorderBrush="Transparent"  Command="{Binding InsertPersonsBonus}"/>
        </Border>
        <Border BorderThickness="1" CornerRadius="5" Background="#F48473" Height="100" Grid.Row="2" Margin="5">
            <Button Content="Others"  FontSize="20" FontWeight="Bold" Background="Transparent" Foreground="white" BorderBrush="Transparent"  Command="{Binding }"/>
        </Border>
        <Grid Grid.Column="1" Grid.RowSpan="3" Margin="5"  Height="auto">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="1.5*"/>
                <RowDefinition Height="3*"/>
            </Grid.RowDefinitions>
            <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,5">
                <TextBlock Text="Search User:" Foreground="white" DockPanel.Dock="Left" FontWeight="Bold" VerticalAlignment="Center" FontSize="18" Margin="10,0,0,0"/>
                <Border Background="White" CornerRadius="15" DockPanel.Dock="Right" Width="300" Height="35" >
                    <TextBox Background="Transparent" Foreground="#F48473" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" VerticalContentAlignment="Center" FontSize="18"/>
                </Border>
            </DockPanel>
            <ListBox Grid.Row="2" ItemsSource="{Binding Path=FilteredListPerson}" HorizontalContentAlignment="Stretch" SelectedItem="{Binding SelectedWorker, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Height="auto" Panel.ZIndex="99" >
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="5" Height="30" >
                            <DockPanel >
                                <TextBlock  Text="{Binding P_Name}" Foreground="White" VerticalAlignment="Center" Margin="10,0,0,0"/>
                            </DockPanel>
                        </Border>

                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" >
                    <TextBlock Text="Name: " Foreground="white"   FontSize="20"/>
                    <TextBlock Text="Function: " Foreground="white" FontSize="20"/>
                    <TextBlock Text="Contract: " Foreground="white"  FontSize="20"/>
                    <TextBlock Text="Last Bonus Date: " Foreground="white" FontSize="20"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="5">
                    
                    <TextBlock Text="{Binding SelectedWorker.P_Name}" Foreground="white" FontSize="20"/>
                    <TextBlock Text="{Binding SelectedWorker.P_Role}" Foreground="white" FontSize="20"/>
                    <TextBlock Text="{Binding SelectedWorker.P_Contract}" Foreground="white" FontSize="20"/>
                    <TextBlock Text="00/00/00" Foreground="white" FontSize="20"/>
                </StackPanel>
                <DockPanel Margin="5">
                    
                    
                </DockPanel>
                <DockPanel Margin="5">
                   
                    
                </DockPanel>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
