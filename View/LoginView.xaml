<UserControl x:Class="MVVM_Bonus.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVM_Bonus"
             mc:Ignorable="d" Height="139" Width="304" >
    <StackPanel Margin="5">
        <TextBlock Foreground="white" Text="Login:"  Margin="5"/>
        <Border Margin="5" Background="White" CornerRadius="20" >
            <TextBox Foreground="black" BorderThickness="0" Height="40" VerticalContentAlignment="Center" Background="Transparent" Text="{Binding LoginName, UpdateSourceTrigger=PropertyChanged}"   />
        </Border>
        <Border BorderThickness="1" CornerRadius="5" Background="#F48473" Height="50">
            <Button Content="Login" Background="Transparent" Foreground="white" BorderBrush="Transparent"  Command="{Binding LoginCommand}"/>
        </Border>
        


    </StackPanel>
</UserControl>
 