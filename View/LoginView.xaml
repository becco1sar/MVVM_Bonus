<UserControl x:Class="MVVM_Bonus.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVM_Bonus"
             mc:Ignorable="d" Height="139" Width="304" >
    <StackPanel Margin="5">
        <DockPanel>
            <Label Foreground="white" Content="👤:"  Margin="5"/>

            <TextBox  Margin="5"  Text="{Binding TeamLeaderUserName, UpdateSourceTrigger=PropertyChanged}"/>
        </DockPanel>
       
      
        <Button Content="Login" Command="{Binding LoginCommand}" Style="{StaticResource TransformedButtonBorder}"/>
    </StackPanel>
</UserControl>
 